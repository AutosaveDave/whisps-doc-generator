import React from "react";
import { Typography, Box, Container } from "@mui/material";

export default function WhispsSpawn( spawnTop, spawnRight, spawnBottom, spawnLeft ) {
    return ( <>
        <Box>
            
        </Box>
    </> );
}